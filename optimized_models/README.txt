Optimized models are saved here: everytime a new optuna hyperparameter
tuning is run, a new model is saved. The model (usually actually a whole
pipeline) is the one that had the best params during the tuning.